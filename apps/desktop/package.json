{
  "name": "@nexus/desktop",
  "private": true,
  "version": "0.1.0",
  "description": "NexusResearch 桌面壳 —— Electron 主进程，管理窗口与 Python sidecar",
  "main": "dist/main/index.js",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "compile": "node scripts/compile.mjs",
    "build": "node scripts/build.mjs",
    "package": "node scripts/build.mjs && cross-env CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "electron-updater": "^6.3.9"
  },
  "devDependencies": {
    "@types/node": "^24.12.3",
    "cross-env": "^10.1.0",
    "electron": "^33.2.0",
    "electron-builder": "^25.1.8",
    "esbuild": "^0.25.0",
    "typescript": "~6.0.2"
  }
}
